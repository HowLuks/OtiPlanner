version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: meu-saas-app
    ports:
      - "3030:3030"
    command: npm start
    environment:
      - NODE_ENV=production
      - PORT=3030

volumes:
  node_modules:
